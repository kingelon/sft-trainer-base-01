# direnv + conda activation
CONDA_BASE="$(conda info --base 2>/dev/null || true)"
if [ -n "$CONDA_BASE" ]; then
  . "$CONDA_BASE/etc/profile.d/conda.sh"
  conda activate "finetune_base_env"
else
  echo "conda not found; please install or adjust PATH" >&2
fi
export PATH="$CONDA_PREFIX/bin:$PATH"
# export HF_HUB_ENABLE_HF_XET=1
# export MY_CIVIT_TOKEN=26c8eb514d889a971ef62d964810fe1f
# optional: add local scripts to PATH
# if [ -d ./scripts ]; then
#   PATH_add ./scripts
# fi

# # Load centralized environment variables (frontend + backend).
# if [ -f .secrets/.env ]; then
#   set -a
#   . .secrets/.env
#   set +a
# fi
